# FaceForge · 任务拆解文档(Phase → Task)

> 版本 v0.1 · 2026-07-28 · 按 Phase → Task 顺序推进,每 Task 单 commit + push,先写测试。
> 沙箱无 GPU:标 🖥️GPU 的 Task 只能在有卡机器验证真实推理,沙箱内只保证代码 + mock 单测。

## Phase 0 · 文档先行 ✅(本阶段)
- [x] T0.1 PRD 产品需求文档
- [x] T0.2 技术架构文档
- [x] T0.3 任务拆解文档(本文件)
- [x] T0.4 任务进度文档

## Phase 1 · 项目脚手架 ✅
- [x] T1.1 monorepo 基座:`package.json`、`pnpm-workspace.yaml`、`turbo.json`、`.gitignore`、`.env.example`、根 README
  - Acceptance:`pnpm install` 成功;`pnpm lint/test/type-check` 空跑通过(无源码也不报错)
  - Verify:`pnpm install && pnpm -w lint`
- [x] T1.2 `apps/web` 脚手架:Next.js + TS + Tailwind + Vitest,一个占位首页
  - Verify:`pnpm --filter web test`、`type-check`、`build`
- [x] T1.3 `services/api` 脚手架:FastAPI + pytest + ruff + mypy,健康检查 `GET /health`
  - Verify:`pytest`、`ruff check`、`mypy`
- [x] T1.4 `services/face-engine` 脚手架:Python 包 + pytest + ruff + mypy,`__init__` + 空 schemas
  - Verify:`pytest`
- [x] T1.5 `docker/`:GPU Dockerfile(api+engine)、web Dockerfile、`docker-compose.yml`(模型卷)—— 🖥️GPU 构建

## Phase 2 · face-engine 核心(全 mock 单测)✅(2.6 待真机)
- [x] T2.1 `engine/schemas.py`:质量参数、人脸选择、任务状态等 pydantic 模型 + 单测
- [x] T2.2 `engine/models.py`:模型清单/下载/校验/缓存 逻辑(下载可 mock),启动自检 + 单测
- [x] T2.3 `engine/facefusion_runner.py`:封装 FaceFusion 图片换脸(命令组装 + 结果解析,subprocess mock)+ 单测
- [x] T2.4 视频换脸:`create_video_job` + 任务状态机(pending→running→done/failed,进度)+ 单测
- [x] T2.5 `engine/realtime.py`:实时换脸接口(吃帧吐帧,引擎 mock)+ 单测
- [ ] T2.6 🖥️GPU:接真实 FaceFusion/Deep-Live-Cam,三场景真机跑通并留证

## Phase 3 · API 网关 ✅
- [x] T3.1 REST:`POST /swap/image` + 单测(engine mock)
- [x] T3.2 REST:`POST /swap/video`(建 job)+ `GET /jobs/{id}`(进度/结果)+ 队列(内存)+ 单测
- [x] T3.3 WS:`/ws/realtime`(收发帧)+ 单测
- [x] T3.4 模型状态 `GET /models` + 错误处理/入参校验 + 单测

## Phase 4 · Web 前端
- [ ] T4.1 API client + shared-types(与 python schema 对齐)+ 单测
- [ ] T4.2 图片换脸页(上传/人脸选择/参数/结果)+ 组件单测
- [ ] T4.3 视频换脸页(上传/进度/下载)+ 单测
- [ ] T4.4 实时换脸页(webcam + WS)+ 单测
- [ ] T4.5 UI 打磨(apple-design → frontend-design)+ 响应式/可访问性

## Phase 5 · 部署 + 端到端
- [ ] T5.1 🖥️GPU:`docker compose up` 起全栈,三功能端到端跑通,写部署文档 + 留证
- [ ] T5.2 README 完整化(架构图、快速开始、参数说明、合规声明)

## Phase 6 · 增强(P1)
- [ ] T6.1 批处理 + 结果历史
- [ ] T6.2 溯源水印(C2PA)+ 审计日志
- [ ] T6.3 参数预设(质量/速度)+ 并发/队列(Redis/RQ)
