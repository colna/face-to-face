# FaceForge — 任务进度

> 自动更新,每完成一个 Task 追加记录。格式:状态 / 完成时间 / 测试 / commit / 备注。

## Phase 0: 文档先行

### Task 0.1~0.4 — PRD / 技术架构 / 任务拆解 / 任务进度 文档
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:N/A(纯文档)
- **commit**:（见本次 docs 提交）
- **备注**:创造模式「文档先行」。关键决策:FaceFusion(图/视频)+ Deep-Live-Cam(实时)编排;monorepo(web/api/face-engine);沙箱无 GPU → GPU 推理相关 Task 标 🖥️,仅在有卡机器验证真实推理,沙箱内保证代码 + mock 单测。

## Phase 1: 项目脚手架

### Task 1.1 — monorepo 基座(package.json / pnpm-workspace / turbo.json / .npmrc)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:`pnpm install` 成功;`pnpm lint`/`test` 空跑 exit 0
- **commit**:（本次提交）
- **备注**:pnpm + Turborepo 基座;workspaces = apps/* + packages/*(services/* 为 Python,不入 pnpm workspace)。

### Task 1.4 — services/face-engine 脚手架(Python 包 + pytest/ruff/mypy)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:pytest 1 passed;ruff All checks passed;mypy no issues
- **备注**:`engine` 包 + 占位 `schemas.py`;pydantic 依赖就绪,待 T2.1 填充。

### Task 1.3 — services/api 脚手架(FastAPI + GET /health)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:pytest 1 passed(TestClient /health=200);ruff passed;mypy no issues
- **备注**:FastAPI app + 健康检查;httpx TestClient 单测就绪。

### Task 1.2 — apps/web 脚手架(Next.js 15 + React 19 + TS + Tailwind + Vitest)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:vitest 1 passed;tsc --noEmit 0 err;next build 成功;next lint 0 warn
- **坑**:用户全局 shell `NODE_ENV=production` → React 走生产构建、`React.act` 缺失,vitest 报 `React.act is not a function`。修法:test 脚本前置 `NODE_ENV=test`。
- **备注**:占位首页 h1「FaceForge」;Tailwind + App Router。

### Task 1.5 — docker/(GPU Dockerfile + compose)🖥️
- **状态**:✅ 代码完成(真实构建需 GPU 机器)
- **完成时间**:2026-07-28
- **测试**:compose YAML 语法校验通过(sandbox 无 docker,构建/运行留待有卡机器)
- **备注**:engine/api 基于 `nvidia/cuda:12.4.1-cudnn-runtime`;web 走 Next standalone(node:22,已加 `output:'standalone'`);compose 声明 nvidia GPU device + models 卷。onnxruntime-gpu/facefusion/deep-live-cam 等大依赖在有卡机器安装。

## Phase 2: face-engine 核心(全 mock 单测)

### Task 2.1 — engine/schemas.py(质量/人脸/任务状态 pydantic 模型)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:pytest 8 passed;ruff passed;mypy clean(TDD 先写 8 条测试)
- **坑**:py3.9 运行期 pydantic 无法 eval PEP604 `int | None` → 用 `typing.Optional`,并让 ruff 忽略 FA100(engine + api 两侧 pyproject 同步)。
- **备注**:SwapQuality/FaceSelection/ImageSwapRequest/VideoSwapRequest(trim 校验)/JobState(进度 0-1)/枚举 FaceEnhancer·FaceSelectorMode·JobStatus。

### Task 2.2 — engine/models.py(模型清单/下载/校验/缓存 + 自检)
- **状态**:✅ 完成
- **完成时间**:2026-07-28
- **测试**:pytest 14 passed;ruff passed;mypy clean(下载器注入 mock,不触网)
- **备注**:ModelSpec + MODEL_CATALOG(hyperswap/codeformer/face_parser,sha 占位待真机核对);ModelManager 存在性/sha256 完整性校验、按需下载、损坏重下、self_check 启动自检。

### Task 2.3 — engine/facefusion_runner.py(图片换脸封装:命令组装 + 结果校验)
- **状态**:✅ 完成(命令行 flag 待真机核对 🖥️)
- **完成时间**:2026-07-28
- **测试**:pytest 20 passed;ruff passed;mypy clean(subprocess 注入 mock)
- **坑**:测试用 `" ".join(cmd)` 子串判断被 pytest tmp 路径(名含 "occlusion")误伤 → 改 token 级 `in cmd` 判断。
- **备注**:build_image_command 组装 FaceFusion headless-run argv(processors/swapper/pixel-boost/enhancer/blend/遮挡蒙版/人脸选择);swap_image 校验 returncode + 产物存在。

### Task 2.4 — 视频换脸命令 + VideoJobManager 状态机
- **状态**:✅ 完成(逐帧进度解析待真机 🖥️)
- **完成时间**:2026-07-28
- **测试**:pytest 27 passed;ruff passed;mypy clean(swap_fn 注入 mock)
- **备注**:runner 加 build_video_command(裁剪帧 + libx264)/swap_video;engine/jobs.py VideoJobManager 内存状态机 pending→running→done/failed + 进度回调,失败落 error。并发队列/持久化留 P1。

### Task 2.5 — engine/realtime.py(实时换脸会话:吃帧吐帧)
- **状态**:✅ 完成(默认引擎需 GPU 🖥️,沙箱注入 mock)
- **完成时间**:2026-07-28
- **测试**:pytest 32 passed;ruff passed;mypy clean
- **备注**:RealtimeSession 逐帧处理(bytes,便于 WS 传输),frames_processed 计数;未注入处理器时默认走 Deep-Live-Cam,非 GPU 调用抛明确 NotImplementedError。

### Task 2.6 — 接真实 FaceFusion/Deep-Live-Cam(三场景真机)🖥️
- **状态**:⏸ 待 GPU 机器(沙箱无卡,不假称跑通)
- **备注**:图片/视频用 build_*_command 的 argv 接 FaceFusion 3.6 headless;实时注入 Deep-Live-Cam processor。真机需核对 flag、补 MODEL_CATALOG 的真实 URL/sha256,并留三场景产物证据。
